apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    jsonData:
      version: Flux
      organization: network-monitor
      defaultBucket: router_metrics
    secureJsonData:
      token: my-super-secret-auth-token
    isDefault: true
